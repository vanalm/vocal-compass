# Alembic reads DATABASE_URL through vocal_compass.settings (see migrations/env.py),
# so no URL lives here, and logs go through the app's own formatter.
[alembic]
script_location = %(here)s/migrations
prepend_sys_path = %(here)s
path_separator = os
file_template = %%(rev)s_%%(slug)s
