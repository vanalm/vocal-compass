[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
# Warnings raised from our own code fail the run. Dependencies' internal deprecations are only
# reported, so a lock upgrade surfaces them without failing on code that is not ours to change.
filterwarnings =
    error:::(vocal_compass|conftest|support|test_)
